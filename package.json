{
  "name": "scanner",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build:minify": "node scripts/minify-client.js",
    "build:exe": "pkg . --compress GZip --out-path dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "pkg": {
    "assets": ["public/**/*"],
    "targets": ["node20-linux-x64", "node20-win-x64"]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@yao-pkg/pkg": "^5.15.0",
    "terser": "^5.39.2"
  }
}
